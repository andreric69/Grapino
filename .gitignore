node_modules
dist
dist-ssr
*.local
.tsbuild
*.tsbuildinfo

# Von Tesseract.js bei Node-basierten Testlaeufen im Projektordner abgelegt
*.traineddata

.env
.env.local
.env.*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Lokale, maschinenspezifische Session-/Launch-Konfiguration (Claude Code) -
# enthaelt absolute Pfade und Laufzeit-Status, nicht fuer Versionskontrolle.
.claude/

# Lokale Datenbank-Sicherungen (echte, persoenliche Sammlungsdaten) - gehoeren
# nicht in die Versionskontrolle.
backups/

# Notfall-/Wiederherstellungsordner - enthaelt echte Zugangsdaten
# (service_role-Schluessel, Admin-Passwort etc.), darf niemals committet werden.
NOTFALL/

# Claude-Arbeitsordner fuer temporaere Dateien (z. B. Artefakt-Entwuerfe) -
# kein Projektinhalt.
scratchpad/

# Lokal per "npx skills add" installierte Referenz-Skills (z. B. Stripe-Doku)
# - Werkzeug-Hilfsmittel fuer die Entwicklung, kein Projektinhalt.
.agents/
skills-lock.json
